{{- range .Values.teams }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .name }}-svc
  labels:
    app: {{ .name }}
spec:
  ports:
    - port:  80 
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ .name }}
